import { PurchaseOrderItem } from './purchase-order-item';

describe('PurchaseOrderItem', () => {
  it('should create an instance', () => {
    expect(new PurchaseOrderItem()).toBeTruthy();
  });
});
